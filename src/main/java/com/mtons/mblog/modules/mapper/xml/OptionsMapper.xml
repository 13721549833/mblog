<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mtons.mblog.modules.mapper.OptionsMapper">

    <select id="findAll" resultType="com.mtons.mblog.modules.pojo.Options">
        SELECT id, key_, type, `value` FROM mto_options
    </select>
</mapper>